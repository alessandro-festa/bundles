diff --git a/packages/opensearch/3/templates/_helpers.tpl b/packages/opensearch/3/templates/_helpers.tpl
index f7dc47d..cc747df 100755
--- a/packages/opensearch/3/templates/_helpers.tpl
+++ b/packages/opensearch/3/templates/_helpers.tpl
@@ -104,10 +104,10 @@ app.kubernetes.io/instance: {{ .Release.Name }}
 {{- end }}

 {{- define "opensearch.dockerRegistry" -}}
-{{- if eq .Values.global.dockerRegistry "" -}}
+{{- if and (eq .Values.global.dockerRegistry "") (eq .Values.global.imageRegistry "") -}}
   {{- .Values.global.dockerRegistry -}}
 {{- else -}}
-  {{- .Values.global.dockerRegistry | trimSuffix "/" | printf "%s/" -}}
+  {{- (coalesce .Values.global.imageRegistry .Values.global.dockerRegistry ) | trimSuffix "/" | printf "%s/" -}}
 {{- end -}}
 {{- end -}}

diff --git a/packages/opensearch/3/templates/statefulset.yaml b/packages/opensearch/3/templates/statefulset.yaml
index d87e472..26dc0fa 100644
--- a/packages/opensearch/3/templates/statefulset.yaml
+++ b/packages/opensearch/3/templates/statefulset.yaml
@@ -249,7 +249,7 @@ spec:
       initContainers:
 {{- if and .Values.persistence.enabled .Values.persistence.enableInitChown }}
       - name: fsgroup-volume
-        image: "{{ template "opensearch.dockerRegistry" . }}{{ .Values.persistence.image | default "busybox" }}:{{ .Values.persistence.imageTag | default "latest" }}"
+        image: "{{ coalesce (include "opensearch.dockerRegistry" .) .Values.persistence.image.registry }}/{{ .Values.persistence.image.repository }}:{{ .Values.persistence.image.tag }}"
         imagePullPolicy: "{{ .Values.image.pullPolicy }}"
         command: ['sh', '-c']
         args:
@@ -264,7 +264,7 @@ spec:
 {{- end }}
 {{- if .Values.sysctlInit.enabled }}
       - name: sysctl
-        image: "{{ template "opensearch.dockerRegistry" . }}{{ .Values.sysctlInit.image | default "busybox" }}:{{ .Values.sysctlInit.imageTag | default "latest" }}"
+        image: "{{ coalesce (include "opensearch.dockerRegistry" .) .Values.sysctlInit.image.registry }}/{{ .Values.sysctlInit.image.repository }}:{{ .Values.sysctlInit.image.tag }}"
         imagePullPolicy: "{{ .Values.image.pullPolicy }}"
         command:
         - sh
@@ -284,7 +284,7 @@ spec:
 {{- end }}
 {{- if .Values.config }}
       - name: configfile
-        image: "{{ template "opensearch.dockerRegistry" . }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
+        image: "{{ coalesce (include "opensearch.dockerRegistry" .) .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion}}"
         imagePullPolicy: "{{ .Values.image.pullPolicy }}"
         command:
         - sh
@@ -305,7 +305,7 @@ spec:
 {{- end }}
 {{- if .Values.keystore }}
       - name: keystore
-        image: "{{ template "opensearch.dockerRegistry" . }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
+        image: "{{ coalesce (include "opensearch.dockerRegistry" .) .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion}}"
         imagePullPolicy: "{{ .Values.image.pullPolicy }}"
         command:
         - sh
@@ -378,7 +378,7 @@ spec:
           bash opensearch-docker-entrypoint.sh
       {{- end }}

-        image: "{{ template "opensearch.dockerRegistry" . }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
+        image: "{{ coalesce (include "opensearch.dockerRegistry" .) .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion}}"
         imagePullPolicy: "{{ .Values.image.pullPolicy }}"
         readinessProbe:
 {{ toYaml .Values.readinessProbe | indent 10 }}
